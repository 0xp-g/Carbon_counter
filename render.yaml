databases:
  - name: carbon-counter-db
    databaseName: carbon_counter_db
    user: postgres
    plan: free

services:
  # 1. The FastAPI Backend
  - type: web
    name: carbon-counter-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    plan: free
    healthCheckPath: /
    envVars:
      # This correctly references the database defined above
      - key: DATABASE_URL
        fromDatabase:
          name: carbon-counter-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true

  # 2. The React Frontend (Defined as a Static Site)
  - type: static_site # Corrected type
    name: carbon-counter-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./frontend/dist
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: carbon-counter-backend
          property: url
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
